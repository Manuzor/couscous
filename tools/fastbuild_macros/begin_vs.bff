
// Call this macro at some point in the global scope,
// before calling begin_target.bff
#define MY_BEGIN_VS

.MyTargetList = {}
.SolutionConfigs = {}
.SolutionProjects = {}
.SolutionFolders = {}

// Even though the exact platform toolset is irrelevant for us,
// we specify it (globally) to shut up warnings in Visual Studio.
.PlatformToolset = 'v$VSVersionMajor$0'
